on: [push]

jobs:
  steps:
  - uses: actions/checkout@latest
  - uses: actions/setup-java@v1
    with:
      java-version: '11'
      java-package: jdk
      architecture: x64
  - uses: DeLaGuardo/setup-clojure@master
    with:
      lein: 2.9.4
  - run: lein cljsbuild
